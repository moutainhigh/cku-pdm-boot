<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ckunion.modules.materials.mapper.BasUserOrgMapper">

    <!--  根据内勤查询外勤 -->
    <!--<select id="getUserByUser" resultType="org.jeecg.modules.system.entity.SysUser">
        SELECT user.* FROM `bas_user_org` org left join sys_user user on org.wai_user=user.username
        where nei_user=#{neiuser}
        <if test="username!=null and username!=''">
            and username = #{username}
        </if>
    </select>-->

</mapper>